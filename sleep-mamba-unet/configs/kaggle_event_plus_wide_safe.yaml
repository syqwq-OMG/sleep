base_config: configs/kaggle_event_plus.yaml

training:
  epochs: 10
  batch_size: 1
  window_size: 7200
  stride: 7200
  folds: 2

debug:
  n_series: 240
  max_windows_per_series: 8
  series_sample: random
  window_sampling: event_focused
  event_window_fraction: 0.8

outputs:
  root: /kaggle/working/outputs_event_plus_wide_safe
